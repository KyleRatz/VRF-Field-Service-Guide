views.fields.html=`<p class="muted">Live availability is calculated from the SMGSL calendar across <b>8 fields</b>. Fields are <b>unavailable Monday and Saturday</b>. Tuesday–Friday the complex is available from <b>6:00 PM–9:00 PM</b>; Sunday from <b>9:00 AM–9:00 PM</b>. Normal practice slots are <b>1.5 hours</b>. SI blocks are treated as <b>3 hours</b>.</p><button class="btn primary" id="refreshFields">Refresh Calendar</button><div id="fieldResults"><div class="empty">Loading field calendar…</div></div>`;
views.si.html=`<div class="warn">SI is tracked separately and is <b>not part of the SMGSL organization</b>. SI practices are treated as <b>3-hour blocks</b>. Usage totals below are calculated from the live calendar over the same 21-day window.</div><button class="btn primary" id="refreshSI">Refresh Calendar</button><div id="siResults"><div class="empty">Loading SI usage…</div></div>`;
function usageSummary(d){const u=d.usage||{};return `<div class="usage-summary"><div class="rule-card"><div class="rule-topic">SI FIELD USAGE</div><div class="rule-question">${u.siFieldHours??0} field-hours</div><div class="small">Next ${u.windowDays||21} days • counts each occupied field separately</div></div><div class="rule-card"><div class="rule-topic">SMGSL FIELD USAGE</div><div class="rule-question">${u.smgslFieldHours??0} field-hours</div><div class="small">Next ${u.windowDays||21} days • counts each occupied field separately</div></div></div>`;}
loadFields=async function(target,siOnly=false){const el=document.querySelector(target);if(!el)return;el.innerHTML='<div class="empty">Refreshing calendar…</div>';try{const r=await fetch('/api/fields');const d=await r.json();if(!r.ok)throw new Error(d.error||'Calendar error');const totals=usageSummary(d);if(siOnly){if(!d.si.length){el.innerHTML=totals+'<div class="empty">No SI-labeled events found in the current 21-day window.</div>';return;}const groups={};d.si.forEach(x=>{const day=new Date(x.start).toLocaleDateString([], {weekday:'long',month:'short',day:'numeric'});(groups[day]??=[]).push(x)});el.innerHTML=totals+Object.entries(groups).map(([day,arr])=>{arr.sort((a,b)=>(a.fieldNumber||99)-(b.fieldNumber||99)||new Date(a.start)-new Date(b.start));return `<div class="field-date">${day}</div>`+arr.map(x=>`<div class="usage"><b>${x.field}</b><span>${new Date(x.start).toLocaleTimeString([], {hour:'numeric',minute:'2-digit'})} – ${new Date(x.end).toLocaleTimeString([], {hour:'numeric',minute:'2-digit'})}<br><span class="small">${(x.durationMinutes/60).toFixed(1)} hr block${x.summary?` • ${x.summary}`:''}${x.location?` • ${x.location}`:''}</span></span></div>`).join('');}).join('');return;}const groups={};d.slots.forEach(x=>{const day=new Date(x.start).toLocaleDateString([], {weekday:'long',month:'short',day:'numeric'});(groups[day]??=[]).push(x)});el.innerHTML=totals+Object.entries(groups).map(([day,arr])=>{arr.sort((a,b)=>new Date(a.start)-new Date(b.start)||(a.fieldNumber||99)-(b.fieldNumber||99));const byTime={};arr.forEach(x=>{const key=new Date(x.start).toLocaleTimeString([], {hour:'numeric',minute:'2-digit'});(byTime[key]??=[]).push(x)});return `<div class="field-date">${day}</div>`+Object.entries(byTime).map(([time,slots])=>{slots.sort((a,b)=>a.fieldNumber-b.fieldNumber);const nums=slots.map(x=>x.fieldNumber).join(', ');const end=new Date(slots[0].end).toLocaleTimeString([], {hour:'numeric',minute:'2-digit'});return `<div class="slot"><b>${time} – ${end}</b><span>Available: <b>Fields ${nums}</b></span></div>`;}).join('');}).join('')||totals+'<div class="empty">No 1.5-hour practice slots are open in the selected windows.</div>';}catch(e){el.innerHTML=`<div class="warn">Could not read the calendar: ${e.message}</div>`;}};
